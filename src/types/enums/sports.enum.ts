export enum SportsSeason {
  FALL = 'fall',
  WINTER = 'winter',
  SPRING = 'spring',
  SUMMER = 'summer',
  YEAR_ROUND = 'year-round',
}

export enum SportsProgramType {
  COMPETITIVE = 'competitive',
  RECREATIONAL = 'recreational',
  BOTH = 'both',
}

export enum SportsEventType {
  PRACTICE = 'practice',
  GAME = 'game',
  MATCH = 'match',
  TOURNAMENT = 'tournament',
  SCRIMMAGE = 'scrimmage',
  MEETING = 'meeting',
  OTHER = 'other',
}

export enum SportsVenue {
  HOME = 'home',
  AWAY = 'away',
  NEUTRAL = 'neutral',
}

export enum SportsAttendanceStatus {
  PRESENT = 'present',
  ABSENT = 'absent',
  LATE = 'late',
  EXCUSED = 'excused',
  MEDICAL = 'medical',
}

export enum SportsAbsenceType {
  EXCUSED = 'excused',
  UNEXCUSED = 'unexcused',
}

export enum StudentSportsStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
  GRADUATED = 'graduated',
  TRANSFERRED = 'transferred',
}

export enum PlayerRole {
  STARTER = 'starter',
  SUBSTITUTE = 'substitute',
  RESERVE = 'reserve',
  TRAINEE = 'trainee',
}

export enum MedicalClearanceStatus {
  CLEARED = 'cleared',
  PENDING = 'pending',
  NO_RECORD = 'no_record',
}

